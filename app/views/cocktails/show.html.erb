<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1><%= @cocktail.name %></h1>
      <%= link_to "Back to all cocktails", root_path %>
    </div>
  </div>
</div>

<%= link_to cocktail_path(@cocktail), class: "btn btn-danger",
       method: :delete, data: { confirm: "Are you sure?" } do %>
       <i class="far fa-trash-alt"></i>
       <% end %>
